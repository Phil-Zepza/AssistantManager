{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "pipeline/Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "sh db/migrate.sh",
    "cronSchedule": "0 6 * * *",
    "restartPolicyType": "NEVER"
  }
}
